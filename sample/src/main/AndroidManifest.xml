<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.drakeet.multitype.sample">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/normal_app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup, GoogleAppIndexingWarning">
        <activity android:name=".qin.QinActivity"></activity>
        <activity android:name=".bilibili.BilibiliActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".selectable.MultiSelectableActivity" />
        <activity android:name=".normal.NormalActivity" />
        <activity android:name=".weibo.WeiboActivity" />
        <activity android:name=".communication.CommunicateWithBinderActivity" />
        <activity android:name=".one2many.OneDataToManyActivity" />
        <activity android:name=".payload.TestPayloadActivity" />
        <activity android:name=".more.MoreApisPlayground" />
    </application>

</manifest>